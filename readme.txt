To run the test and application, type the following commaond from project folder:

	mvn test && mvn spring-boot:run


Please add an empty line to the input at the end for console to detect end of input.

Thanks a lot